const Intern = function(id, name, description, phoneNumber) {

    this.name = name;

    this.description = description;

    this.phoneNumber = phoneNumber;

    this.id = id;


};

modules.export = Intern;